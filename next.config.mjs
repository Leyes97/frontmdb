/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['images.unsplash.com', 'placehold.co','image.tmdb.org'], // Dominios permitidos para cargar imágenes
  },
};

export default nextConfig;
