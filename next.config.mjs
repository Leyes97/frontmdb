/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['images.unsplash.com'], // Dominios permitidos para cargar imágenes
    },
  };
  
  export default nextConfig;